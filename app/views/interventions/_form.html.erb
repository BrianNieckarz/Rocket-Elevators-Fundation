<%= form_with(model: intervention, local: true) do |form| %>
  <% if intervention.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(intervention.errors.count, "error") %> prohibited this intervention from being saved:</h2>
      <ul>
        <% intervention.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


<div class = "container">
<h3>
      <span style="color:rgb(10, 101, 160); font-weight:bolder"></span> Welcome 
      </h3>
  <%= form.text_field(:author_id, :readonly=>true, :value =>@author) %>
</div>

<div class="card card-default">
    <div class="card-heading card-heading-transparent">
      <label for="building-type" class="card-title">
        <h3>
          <span style="color:rgb(10, 101, 160); font-weight:bolder">STEP 1 |</span> Select Customer Name
        </h3>
      </label>
    </div>
    <div class="card-block" style="margin: 20px" <%= form.select(:customer_id) do %> >
        <option selected>customer</option>
        <% @customer.each do |c| -%>
          <option value="<%= c.id %>"><%= c.company_name %></option>
        <% end %>
      <% end %>
    </div>
  </div>

<div class="card card-default">
    <div class="card-heading card-heading-transparent">
      <label for="building-type" class="card-title">
        <h3>
          <span style="color:rgb(10, 101, 160); font-weight:bolder">STEP 2 |</span> Select Building ID
        </h3>
      </label>
    </div>
    <div class="card-block" style="margin: 20px" >
      <%= form.select(:building_id) do %>
        <option selected>building</option>
        <% @building.each do |b| -%>
          <option value="<%= b.id %>"><%= b.customer_id %></option>
        <% end %>
      <% end %>
    </div>
  </div>


  <div class="card card-default">
    <div class="card-heading card-heading-transparent">
      <label for="building-type" class="card-title">
        <h3>
          <span style="color:rgb(10, 101, 160); font-weight:bolder">STEP 3 |</span> Select Battery ID
        </h3>
      </label>
    </div>
    <div class="card-block" style="margin: 20px" >
      <%= form.select(:battery_id) do %>
        <option selected>battery</option>
        <% @battery.each do |b| -%>
          <option value="<%= b.id %>"><%= b.building_id %></option>
        <% end %>
      <% end %>
    </div>
  </div>

  
  <div class="card card-default">
    <div class="card-heading card-heading-transparent">
      <label for="building-type" class="card-title">
        <h3>
          <span style="color:rgb(10, 101, 160); font-weight:bolder">STEP 4 |</span> Select Column ID
        </h3>
      </label>
    </div>
    <div class="card-block" style="margin: 20px" >
      <%= form.select(:column_id) do %>
        <option selected>column</option>
        <% @column.each do |c| -%>
          <option value="<%= c.id %>"><%= c.battery_id %></option>
        <% end %>
      <% end %>
    </div>
  </div>


  <div class="card card-default">
    <div class="card-heading card-heading-transparent">
      <label for="building-type" class="card-title">
        <h3>
          <span style="color:rgb(10, 101, 160); font-weight:bolder">STEP 4 |</span> Select Elevator ID
        </h3>
      </label>
    </div>
    <div class="card-block" style="margin: 20px" >
      <%= form.select(:elevator_id) do %>
        <option selected>elevator</option>
        <% @elevator.each do |e| -%>
          <option value="<%= e.id %>"><%= e.column_id %></option>
        <% end %>
      <% end %>
    </div>
  </div>


  <div class="card card-default">
    <div class="card-heading card-heading-transparent">
      <label for="building-type" class="card-title">
        <h3>
          <span style="color:rgb(10, 101, 160); font-weight:bolder">STEP 5 |</span> Select Employee ID
        </h3>
      </label>
    </div>
    <div class="card-block" style="margin: 20px" >
      <%= form.select(:employee_id) do %>
        <option selected>employee</option>
        <% @employee.each do |e| -%>
          <option value="<%= e.id %>"><%= e.user_id %></option>
        <% end %>
      <% end %>
    </div>
  </div>


  <div class="actions">
    <%= form.submit %>
  </div>

<% end %>
